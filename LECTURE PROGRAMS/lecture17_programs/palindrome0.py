#lecture 18 exercise: Palindrome test
#palindrome: "mom" "dad" ...

#precondition: inputString has no non-letter characters 
# and has uniform case (e.g., all lowercase)
#postcondition:  returns true if inputString is a palindrome,
# false otherwise


#Assumes inputString is all one case and has no spaces
#recall aList[-1] returns the last item in the list
#       aString[-1] returns the last letter in the string
#       aString[s:e] returns sub-string from (s+1)th letters to (e-1)th letters 
def palindrome(inputString):

    #if inputString has only one letter or no letters at all

    #else, if the first letter matches the last letter

    #else if the two end letters didn't even match


    #dummy return
    return True


palindrome("dad")
